# Concept: Platform — Computational Substrate
## Olog: Platform → Location "hosts at". Cross-pillar: Platform ↔ Travel.Types.Lifecycle ("platforms have lifecycles").
## AbstractPlatform with boot(), shutdown(), status(). Platform is where abstract lifecycle transitions become concrete state changes.
## Sub-objects: Local (this process — where DuckDB runs), System (OS), Realm (isolated domain), Sandbox (constrained subset).
## Categorical role: Platform IS the site where the instance functor I: C_Brain → Set meets the physical machine.
