# Concept: External — A Location Outside This System
## Olog: External → Location "extends".
## AbstractExternal with connect(), disconnect(). External locations are reachable VIA coordinates.
## Sub-objects: World (physical/network outside), Domain (named region).
## Cross-pillar: Communication.Language.Dialect ↔ Location.External.Domain ("domains define communication dialects").
