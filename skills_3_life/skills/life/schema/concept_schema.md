# Concept: Schema — The Structure of Records
## Olog: Schema → Life "structures", Schema → Event "constrains".
## AbstractSchema with validate(), render(). Schema defines the shape of events — it is the type in the sense of "an event of this schema." Cross-pillar: Life.Schema ↔ Communication.Language ("schemas follow language rules").
